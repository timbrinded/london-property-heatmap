{
  "name": "london-property-heatmap",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync": "bun run scripts/sync-notion.js",
    "build": "bun run sync && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright": "^1.58.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "@playwright/test": "^1.58.1",
    "mapbox-gl": "^3.18.1"
  }
}
